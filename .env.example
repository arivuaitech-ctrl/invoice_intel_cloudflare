# =========================================================================
# MOBILE APP ENVIRONMENT VARIABLES (.env)
# =========================================================================
# These values are BUILD-TIME variables and will be bundled into your app.
# Only PUBLIC or non-sensitive configuration should be put here.
# =========================================================================

# 1. Supabase (Public)
# These are required for the app to initialize and handle login.
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# 2. Backend URL (Absolute path to your Cloudflare Functions)
# This allows the mobile app to talk to your secure "Security Guard" on Cloudflare.
# Example: https://invoiceintel.pages.dev
VITE_API_URL=https://your-cloudflare-pages-url.dev

# =========================================================================
# NOTE: DO NOT PUT STRIPE_SECRET_KEY OR GEMINI API_KEY HERE!
# Those must stay securely in your Cloudflare Pages Dashboard settings.
# =========================================================================
